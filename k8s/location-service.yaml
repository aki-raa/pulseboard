apiVersion: v1
kind: Secret
metadata:
  name: location-service-secret
  namespace: pulseboard
type: Opaque
stringData:
  DB_USERNAME: root
  DB_PASSWORD: pulseboard_root
  JWT_SECRET: pulseboard-local-jwt-secret-2026-strong-key

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: location-service
  namespace: pulseboard
spec:
  replicas: 1

  selector:
    matchLabels:
      app: location-service

  template:
    metadata:
      labels:
        app: location-service

    spec:
      containers:
        - name: location-service
          image: pulseboard-location-service:latest
          imagePullPolicy: IfNotPresent

          ports:
            - containerPort: 8082

          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8082
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6

          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8082
            initialDelaySeconds: 90
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3

          env:
            - name: DB_URL
              value: jdbc:mysql://mysql:3306/pulseboard

            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: location-service-secret
                  key: DB_USERNAME

            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: location-service-secret
                  key: DB_PASSWORD

            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: location-service-secret
                  key: JWT_SECRET

            - name: KAFKA_BOOTSTRAP_SERVERS
              value: kafka:9092

---
apiVersion: v1
kind: Service
metadata:
  name: location-service
  namespace: pulseboard
spec:
  selector:
    app: location-service

  ports:
    - port: 8082
      targetPort: 8082
